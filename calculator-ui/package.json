{
  "name": "calculator-ui",
  "version": "1.0.0",
  "description": "Presentation layer for the calculator (vanilla HTML/CSS/JS). This package currently provides the UI test tooling (Jest + jsdom) and styling foundation; the runtime markup and handler — a Percentage (%) button that will call the calculator-core API and render the result into #display — are planned for the UI milestone.",
  "scripts": {
    "test": "jest"
  },
  "license": "MIT",
  "jest": {
    "testEnvironment": "jsdom"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1"
  }
}
